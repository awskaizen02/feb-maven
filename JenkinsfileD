pipeline {
    agent any
   

    stages {
        stage('SCM') {
            steps {
                git branch: 'war', url: 'https://github.com/awskaizen02/feb-maven.git'
            }
        }
       stage('build') {
            steps {
                sh 'mvn clean'
                sh 'mvn install'
            }
        }
       }

    }
